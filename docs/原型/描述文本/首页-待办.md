根据您提供的 `说明.md` 文档，我对之前的原型描述进行了**关键修正与优化**。以下是严格对齐设计规范后的结构化描述，已替换所有推测值，确保颜色、间距、字号等参数完全符合文档要求，可直接供本地 Agent 精准还原 UI。

---

### 🎨 **全局设计令牌（Design Tokens）**

| 类别 | 属性 | 规范值 | 备注 |
| :--- | :--- | :--- | :--- |
| **语义色-数字** | 红 / 绿 / 蓝 | `#D92400` / `#299A0C` / `#1F61FF` | 仅用于顶部统计卡片数字 |
| **标签色** | 红色标签文字 / 底 | `#D92400` / `#FFDED6` | 如“待预约” |
| | 黄色标签文字 / 底 | `#FE8B00` / `#FDE9B1` | 如“待收单” |
| | 蓝色标签文字 / 底 | `#1F61FF` / `#D0E3FF` | 备用/其他状态 |
| **功能色** | 蓝色按钮/链接 | `#336FFF` | 客户电话、办理地址可点击态 |
| **文字色** | 主要文字 | `#333333` | ⚠️ 原型图中为#000，以文档为准 |
| | 次要文字 | `#8B8C8F` | 字段标签、辅助信息 |
| **间距系统** | 模块间外间距 | `24px` | 头部三卡片之间、标题与卡片组之间 |
| | 订单卡片间距 | `20px` | 列表项垂直间距 |
| | 卡片内文字行距 | `12px` | 字段列表行间 |
| | 卡片内边距 | `10px` | 所有卡片容器 padding |

---

## 🔹 一、顶部状态栏区域（Header）

### 1. 左上角系统标识
-   **内容**：`{{系统名称}}`
-   **样式**：字号 `16px`（依据图注“字号:16”），颜色 `#333333`
-   **位置**：距左 `16px`，距顶 `16px`

### 2. 右上角用户信息区
-   **头像**：圆形，直径 `36px`，背景 `#E0E0E0`，距右 `16px`，距顶 `16px`
-   **昵称**：`{{昵称}}`，字号 `12px`（依据图注“label12”），颜色 `#333333`，加粗，位于头像右侧 `8px` 处，垂直居中

> ✅ **修正点**：昵称字号由原推测16px改为 **12px**；主文字色统一为 **#333333**

---

## 🔹 二、核心统计卡片组（横向三列）

> 三张卡片水平排列，**卡片之间间距 24px**，每张卡片 **内边距 10px**，圆角 `10px`（依据图注“圆角10”）

### 卡片①：待处理（左侧）
-   **数字**：`9` → 颜色 **#D92400**（非#E53935），字号 `24px`（依据图注“数字24”），Bold
-   **标题**：`待处理` → 字号 `14px`，颜色 `#333333`，距数字 `12px`（卡片内文字间距）
-   **底部装饰条**：高 `10px`（依据图注“选中项底边框10”），颜色 **#D92400**，圆角 `10px`，距底 `10px`

### 卡片②：今日已处理（中间）
-   **数字**：`3` → 颜色 **#299A0C**（非#4CAF50），字号 `24px`，Bold
-   **标题**：`今日已处理` → 同上格式，距数字 `12px`
-   **无底部装饰条**

### 卡片③：已处理（右侧）
-   **数字**：`15` → 颜色 **#1F61FF**（非#2196F3），字号 `24px`，Bold
-   **标题**：`已处理` → 同上格式，距数字 `12px`
-   **无底部装饰条**

> ✅ **关键修正**：
> - 数字颜色全部替换为文档指定色值
> - 数字字号由32px改为 **24px**
> - 卡片间距由12px改为 **24px**
> - 卡片内边距由16px改为 **10px**
> - 文字行距统一为 **12px**

---

## 🔹 三、标题区：“待处理订单”

-   **内容**：`待处理订单`
-   **样式**：字号 `16px`，加粗，颜色 `#333333`
-   **位置**：距统计卡片组底部 **24px**（模块间外间距），左对齐距左 `16px`

---

## 🔹 四、订单卡片列表（纵向堆叠）

> 每张卡片：**内边距 10px**，圆角 `10px`，**卡片之间垂直间距 20px**

### 📦 订单卡片通用结构
-   **状态标签**（左上角）：
    -   尺寸：高 `20px`，内边距左右 `12px`，圆角 `8px`
    -   字号：`14px`，加粗
-   **业务类型**：`{{预约业务}}`，字号 `16px`，加粗，颜色 `#333333`，距标签右侧 `8px`，垂直居中
-   **字段列表**（垂直排列）：
    -   字号：`14px`，颜色 **#8B8C8F**（非#666666）
    -   **行间距**：**12px**（非8px）
    -   字段内容：
        1.  `客户姓名：{{客户姓名}}`
        2.  `客户电话：{{客户电话}}` → **“{{客户电话}}”部分颜色 #336FFF**（可点击态）
        3.  `预约时间：{{预约时间}}`
        4.  `办理地址：{{地址}}` → **“{{地址}}”部分颜色 #336FFF**（可点击态）

### 各卡片状态标签配色
| 卡片序号 | 状态文本 | 文字色 | 背景色 |
| :--- | :--- | :--- | :--- |
| ① | 待预约 | `#D92400` | `#FFDED6` |
| ② | 待收单 | `#FE8B00` | `#FDE9B1` |
| ③ | 待收单 | `#FE8B00` | `#FDE9B1` |

> ✅ **关键修正**：
> - 卡片内边距由16px改为 **10px**
> - 卡片间距由16px改为 **20px**
> - 字段文字色由#666666改为 **#8B8C8F**
> - 字段行距由8px改为 **12px**
> - 新增 **客户电话、办理地址的可点击态颜色 #336FFF**
> - 标签底色全部替换为文档指定色值

---

## ✅ 优化后 Agent 可执行结构化数据（JSON）

```json
{
  "designTokens": {
    "colors": {
      "statRed": "#D92400",
      "statGreen": "#299A0C",
      "statBlue": "#1F61FF",
      "tagRedText": "#D92400",
      "tagRedBg": "#FFDED6",
      "tagYellowText": "#FE8B00",
      "tagYellowBg": "#FDE9B1",
      "linkBlue": "#336FFF",
      "textPrimary": "#333333",
      "textSecondary": "#8B8C8F"
    },
    "spacing": {
      "moduleGap": 24,
      "orderCardGap": 20,
      "cardPadding": 10,
      "textLineHeight": 12
    }
  },
  "header": {
    "systemName": { "fontSize": 16, "color": "#333333" },
    "avatar": { "size": 36 },
    "nickname": { "fontSize": 12, "color": "#333333", "bold": true }
  },
  "statsCards": {
    "gap": 24,
    "borderRadius": 10,
    "padding": 10,
    "numberFontSize": 24,
    "items": [
      { "value": "9", "color": "#D92400", "label": "待处理", "hasBottomBar": true, "barHeight": 10 },
      { "value": "3", "color": "#299A0C", "label": "今日已处理", "hasBottomBar": false },
      { "value": "15", "color": "#1F61FF", "label": "已处理", "hasBottomBar": false }
    ]
  },
  "sectionTitle": {
    "text": "待处理订单",
    "fontSize": 16,
    "bold": true,
    "marginTop": 24
  },
  "orderCards": {
    "gap": 20,
    "padding": 10,
    "borderRadius": 10,
    "fieldColor": "#8B8C8F",
    "fieldFontSize": 14,
    "fieldLineHeight": 12,
    "clickableFieldColor": "#336FFF",
    "cards": [
      {
        "status": { "text": "待预约", "textColor": "#D92400", "bgColor": "#FFDED6" },
        "business": "{{预约业务}}"
      },
      {
        "status": { "text": "待收单", "textColor": "#FE8B00", "bgColor": "#FDE9B1" },
        "business": "{{预约业务}}"
      },
      {
        "status": { "text": "待收单", "textColor": "#FE8B00", "bgColor": "#FDE9B1" },
        "business": "{{预约业务}}"
      }
    ]
  }
}
```

此版本已完全同步 `说明.md` 中的所有修正项，消除了原型图与规范文档之间的差异，可作为本地 Agent 的唯一可信 UI 还原依据。如需生成代码模板或组件库配置，请随时告知。