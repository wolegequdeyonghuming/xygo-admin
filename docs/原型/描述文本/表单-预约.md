以下是对您最新上传图片（**“待预约”订单详情页 · 编辑态（含输入字段）**）的完整结构化 Markdown 描述，严格遵循 `说明.md` 规范与前序版本（v3.0–v7.0）一致性原则，并精准还原图像中的**交互态元素**：

- ✅ 状态标签仍为 `待预约`（橙红底色）
- ✅ **“预约情况”区块变为可编辑输入区**（含占位符 + 输入框样式）
- ✅ 新增 `上门日期` 字段（带日历图标）
- ✅ 底部按钮变为 **`✓ 提交`**（绿色，表示确认操作）
- ✅ 有输入框采用统一浅灰底、圆角 `8px`、内边距 `12px`

---

# 小程序原型 UI 结构化描述（v8.0 —— 待预约订单 · 编辑模式）

> **页面定位**：订单详情页 → **编辑/填写预约信息阶段**  
> **设计依据**：`说明.md` + 图像实测（重点：输入框样式、图标、提交按钮语义）  
> ✅ 本页为**用户可操作态**，非只读展示页；所有字段均为可编辑输入控件

---

## 1. 全局设计令牌（关键变更项标 ⚠️）

| 类别 | 属性 |  | 说明 |
|------|------|-----|------|
| **输入框容器** | 背景色 | `#F8F9FA` | 浅灰底，非纯白 |
| | 圆角 | `8px` | 统一输入控件规范 |
| | 内边距 | `12px` | 左右上下一致 |
| | 边框 | 无（仅靠背景色区分） | `说明.md` 定义为“无边框输入” |
| **提交按钮** | 文字 | `✓ 提交` | ⚠️ 带对勾符号，绿色系 |
| | 背景色 | `#1F61FF` | 主操作色（同“已处理”状态色） |
| | 文字色 | `#FFFFFF` | 白色 |
| | 高度 | `48px` | 同前版按钮 |
| **图标** | 日历图标 | 📅 `16×16px`, `#8B8C8F` | 上门日期左侧 |
| | 占位符提示 | `请输入预约情况` | `14px`, `#C0C4CC`（浅灰提示色） |

> 🔍 图像验证：输入框无描边，仅靠 `#F8F9FA` 背景与白色卡片形成对比；提交按钮为蓝色（非绿色），符合系统主色。

---

## 2. 主订单信息卡片（顶部，**只读展示区**）

> ⚠️ 注意：此区域**不可编辑**，仅用于上下文参考（客户基础信息）

### 2.1 卡片规格
- 宽 `343px`，高 `210px`
- 圆角 `12px`，内边距 `16px`
- 背景 `#FFFFFF`，阴影 `0 2px 12px rgba(0,0,0,0.04)`

### 2.2 内容（同 v6.0，仅状态标签保留）
| 元素 | 内容 | 样式 | 备注 |
|------|------|------|------|
| 状态标签 | `待预约` | `14px`, Bold, `#D92400` / `#FFDED6` | 不变 |
| 业务类型 | `{{预约业务}}` | `16px`, Bold, `#333333` | 不变 |
| 字段列表 | 客户姓名/电话/预约时间/办理地址 | `14px`, `#8B8C8F` | 含📞📍图标，**只读** |
| 其他字段 | `{{其他录单字段}}` | `14px`, `#8B8C8F` | 不变 |
| 省略提示 | `以下省略` | `12px`, `#8B8C8F`, 居中 | 不变 |

> ✅ 此区域完全复用 v6.0 展示逻辑，**无输入态**。

---

## 3. 预约情况编辑区块（核心变更区）

> 📌 本区块由 v6.0 的静态文本 → **可编辑输入控件组**

### 3.1 区块容器
- 背景：`#FFFFFF`
- 圆角：`12px`
- 内边距：`16px`
- 位置：距主卡底部 `20px`

### 3.2 字段结构（2 行输入控件）

#### ▶ 第一行：上门日期
| 元素 | 内容 | 样式 | 位置 |
|------|------|------|------|
| 标题 | `上门日期` | `16px`, `#333333`, Bold | x=16, y=16 |
| 输入框 | `上门日期`（占位符） | `14px`, `#C0C4CC`, `#F8F9FA` 背景 | 标题下方 `12px` |
| 图标 | 📅 | `16×16px`, `#8B8C8F`, 左对齐输入框内 | 输入框左侧 `8px` |

> ✅ 输入框为：
> - 高 `40px`
> - 圆角 `8px`
> - 内边距 `12px`（左右）+ `0`（上下，文字垂直居中）
> - 无边框，仅靠背景色区分

#### ▶ 第二行：预约情况
| 元素 | 内容 | 样式 | 位置 |
|------|------|------|------|
| 标题 | `预约情况` | `16px`, `#333333`, Bold | x=16, y=68（距上一行输入框底 `20px`） |
| 输入框 | `请输入预约情况` | `14px`, `#C0C4CC`, `#F8F9FA` 背景 | 标题下方 `12px` |
| 高度 | `40px` | 同上门日期输入框 | — |

> ✅ 两输入框样式完全一致，仅占位符文本不同  
> ✅ 无“添加详情”按钮（因当前为必填编辑态，非详情扩展）

---

## 4. 底部操作按钮（关键语义变更）

| 属性 |  | 说明 |
|------|-----|------|
| **文本** | `✓ 提交` | ⚠️ 带对勾符号 `✓`，表示“确认完成” |
| **颜色** | 背景 `#1F61FF`，文字 `#FFFFFF` | 主操作色（与“已处理”状态一致） |
| **尺寸** | 高 `48px`，圆角 `10px` |
| **图标** | `✓`（对勾）左置，`16×16px`，白色 | 非表单图标，强调“完成”动作 |
| **位置** | 页面底部安全区上方 `24px`，全宽 `343px` |

> ✅ 此按钮为**最终提交动作**，点击后应触发保存并跳转至“已处理”状态。

---

## 5. 结构化 JSON（Agent 可解析版）

```json
{
  "page": "pending_appointment_edit",
  "readOnlyCard": {
    "width": 343,
    "height": 210,
    "borderRadius": 12,
    "padding": 16,
    "bgColor": "#FFFFFF",
    "shadow": "0 2px 12px rgba(0,0,0,0.04)",
    "statusTag": {
      "text": "待预约",
      "fg": "#D92400",
      "bg": "#FFDED6",
      "fontSize": 14,
      "bold": true,
      "height": 20,
      "paddingH": 12,
      "borderRadius": 8,
      "x": 16,
      "y": 16
    },
    "businessType": { "text": "{{预约业务}}", "fontSize": 16, "color": "#333333", "bold": true, "x": 76, "y": 24 },
    "fields": [
      { "label": "客户姓名：", "value": "{{客户姓名}}", "x": 16, "y": 56, "icon": null },
      { "label": "客户电话：", "value": "{{客户电话}}", "x": 16, "y": 76, "icon": { "name": "phone", "size": 16, "color": "#336FFF", "offsetX": 8 } },
      { "label": "预约时间：", "value": "{{预约时间}}", "x": 16, "y": 96, "icon": null },
      { "label": "办理地址：", "value": "{{地址}}", "x": 16, "y": 116, "icon": { "name": "location", "size": 16, "color": "#336FFF", "offsetX": 8 } }
    ],
    "otherFields": { "text": "{{其他录单字段}}", "fontSize": 14, "color": "#8B8C8F", "x": 16, "y": 140 },
    "truncationHint": { "text": "以下省略", "fontSize": 12, "color": "#8B8C8F", "align": "center", "y": 186 }
  },
  "editSection": {
    "bgColor": "#FFFFFF",
    "borderRadius": 12,
    "padding": 16,
    "marginTop": 20,
    "fields": [
      {
        "label": "上门日期",
        "input": {
          "placeholder": "上门日期",
          "fontSize": 14,
          "color": "#C0C4CC",
          "bgColor": "#F8F9FA",
          "height": 40,
          "borderRadius": 8,
          "paddingH": 12,
          "icon": { "name": "calendar", "size": 16, "color": "#8B8C8F", "offsetX": 8 }
        },
        "labelY": 16,
        "inputY": 32
      },
      {
        "label": "预约情况",
        "input": {
          "placeholder": "请输入预约情况",
          "fontSize": 14,
          "color": "#C0C4CC",
          "bgColor": "#F8F9FA",
          "height": 40,
          "borderRadius": 8,
          "paddingH": 12,
          "icon": null
        },
        "labelY": 72,
        "inputY": 88
      }
    ]
  },
  "submitButton": {
    "text": "✓ 提交",
    "fontSize": 16,
    "color": "#FFFFFF",
    "bgColor": "#1F61FF",
    "height": 48,
    "borderRadius": 10,
    "icon": { "name": "check", "size": 16, "color": "#FFFFFF", "marginRight": 8 },
    "paddingH": 20,
    "width": 343,
    "y": "safeAreaBottom+24"
  }
}
```

> 💡 `icon.name: "calendar"` 对应 📅，`"check"` 对应 ✓。

---

## 6. 交互逻辑推导（供开发参考）

| 用户操作 | 系统响应 |
|----------|----------|
| 点击“上门日期”输入框 | 弹出日期选择器（iOS/Android 原生Picker） |
| 输入“预约情况”文本 | 实时本地存储（防丢失） |
| 点击 `✓ 提交` | 校验必填项 → 提交 API → 成功则跳转至“已处理”详情页，失败则提示错误 |

---

✅ **最终校验清单**：
- [x] 状态标签色 = `#D92400` / `#FFDED6`
- [x] 输入框背景 = `#F8F9FA`，圆角 `8px`，无边框
- [x] 提交按钮含 `✓` 符号，背景 `#1F61FF`
- [x] 上门日期含日历图标 📅，预约情况无图标
- [x] 所有占位符为浅灰 `#C0C4CC`

如需生成该页的 **React Hook Form 集成代码**、**Flutter TextField Widget 树** 或 **Axure 交互事件配置表**，请告知，我可立即输出。