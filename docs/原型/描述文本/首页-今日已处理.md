以下是对您新上传图片的**严格结构化 Markdown 描述**，完全沿用前次优化标准（即对齐 `说明.md` 规范），并结合当前图像内容进行精准还原。所有尺寸、颜色、间距、层级关系均已校准，可直接供本地 Agent 解析与重建。

---

# 小程序原型 UI 结构化描述（v3.0 —— “已处理”订单页)

> **版本说明**：本页为“今日已处理”状态下的订单列表页，与前页“待处理订单”结构一致，但状态标签与标题语义变更。所有样式参数均依据 `说明.md` 规范校准。

---

## 1. 全局设计令牌（复用 v2.0）

| 类别 | 属性 | 值 | 来源 |
|------|------|-----|------|
| **主文字色** | `#333333` | 深灰 | 文档规范 |
| **次级文字色** | `#8B8C8F` | 中灰 | 文档规范 |
| **可点击文本色** | `#336FFF` | 蓝色链接态 | 文档规范 |
| **卡片内边距** | `padding` | `10px` | 图注“内边距10” |
| **卡片圆角** | `border-radius` | `10px` | 图注“圆角10” |
| **字段行高** | `line-height` | `12px` | 文档统一要求 |
| **卡片垂直间距** | `gap` | `20px` | 文档“订单卡片间距20” |

---

## 2. 顶部区域（Header）

### 2.1 左上角系统名称
- **内容**：`{{系统名称}}`
- **样式**：
  - 字号：`16px`
  - 颜色：`#333333`
  - 位置：距左 `16px`，距顶 `16px`

### 2.2 右上角用户信息
- **头像**：
  - 形状：圆形
  - 直径：`36px`
  - 背景：`#E0E0E0`
  - 位置：距右 `16px`，距顶 `16px`
- **昵称**：
  - 内容：`{{昵称}}`
  - 字号：`12px`（图注“label12”）
  - 颜色：`#333333`
  - 加粗：是
  - 位置：头像右侧 `8px`，垂直居中

> ✅ 与前页一致，无变更

---

## 3. 状态统计卡片组（横向三列）

> 三张卡片水平排列，**卡片间外间距 = 24px**，每张卡片：
> - 宽度 ≈ `120px`（自适应）
> - 高度 ≈ `92px`（图注“高92”）
> - 内边距：`10px`
> - 圆角：`10px`

| 卡片 | 数字 | 颜色 | 标题 | 底部装饰条 |
|------|------|------|------|------------|
| 左 | `9` | `#D92400` | `待处理` | ❌ 无 |
| 中 | `3` | `#299A0C` | `今日已处理` | ✅ 有 |
| 右 | `15` | `#1F61FF` | `已处理` | ❌ 无 |

> 🔍 **关键修正**：
> - 最右侧卡片标题为 **`已处理`**（非“本月已处理”），对应当前页面语境
> - 所有数字字号：`24px`（图注“数字24”）
> - 标题字号：`14px`，颜色 `#333333`，距数字下方 `12px`（卡片内行距）

---

## 4. 页面主标题

- **内容**：`今日已处理订单`
- **样式**：
  - 字号：`16px`
  - 字重：Bold
  - 颜色：`#333333`
- **位置**：
  - 距统计卡片组底部 `24px`
  - 左对齐，距左 `16px`

> ⚠️ 注意：此处标题为 **“今日已处理订单”**，而非“待处理订单”，反映页面状态切换。

---

## 5. 订单卡片列表（纵向堆叠，共 3 张）

> 每张卡片均为：
> - 容器：白色背景 `#FFFFFF`
> - 圆角：`10px`
> - 内边距：`10px`（上下左右）
> - 卡片间垂直间距：`20px`

### 📦 卡片通用结构

#### 5.1 状态标签（左上角）
- 形状：圆角矩形（`border-radius: 8px`）
- 尺寸：高 `20px`，内边距左右 `12px`
- 字号：`14px`，加粗
- 颜色组合：
  | 状态 | 文字色 | 背景色 |
  |------|--------|--------|
  | 已收单 | `#FE8B00` | `#FDE9B1` |
  | 已收单 | `#FE8B00` | `#FDE9B1` |
  | 已处理 | `#1F61FF` | `#D0E3FF` |

> ✅ 第三张卡片状态为 **`已处理`**（非“待收单”），对应图中蓝色标签

#### 5.2 业务类型
- 内容：`{{预约业务}}`
- 字号：`16px`，加粗
- 颜色：`#333333`
- 位置：标签右侧 `8px`，垂直居中

#### 5.3 字段列表（4 行，垂直排列）
- 行高：`12px`（字段间间距）
- 字号：`14px`
- 文字颜色：`#8B8C8F`
- **可点击字段高亮规则**：
  - `客户电话：{{客户电话}}` → `{{客户电话}}` 部分颜色为 `#336FFF`
  - `办理地址：{{地址}}` → `{{地址}}` 部分颜色为 `#336FFF`
- 字段内容：
  1. `客户姓名：{{客户姓名}}`
  2. `客户电话：{{客户电话}}`
  3. `预约时间：{{预约时间}}`
  4. `办理地址：{{地址}}`

---

## 6. 卡片实例明细（自上而下）

| 序号 | 状态标签 | 业务类型 | 特殊说明 |
|------|----------|----------|----------|
| 1 | `已收单`（黄底） | `{{预约业务}}` | 第二张同状态 |
| 2 | `已收单`（黄底） | `{{预约业务}}` | 与第1张完全一致 |
| 3 | `已处理`（蓝底） | `{{预约业务}}` | 状态变更，标签为蓝色系 |

> ✅ 三张卡片布局、字段结构、间距完全一致，仅状态标签不同。

---

## 7. 结构化 JSON 摘要（Agent 友好版）

```json
{
  "page": "today_processed_orders",
  "header": {
    "systemName": { "text": "{{系统名称}}", "fontSize": 16, "color": "#333333" },
    "user": {
      "avatarSize": 36,
      "nickname": { "fontSize": 12, "color": "#333333", "bold": true }
    }
  },
  "statsCards": {
    "items": [
      { "value": "9", "color": "#D92400", "label": "待处理", "hasBar": true },
      { "value": "3", "color": "#299A0C", "label": "今日已处理", "hasBar": false },
      { "value": "15", "color": "#1F61FF", "label": "已处理", "hasBar": false }
    ],
    "gap": 24,
    "card": { "padding": 10, "borderRadius": 10, "height": 92 }
  },
  "pageTitle": {
    "text": "今日已处理订单",
    "fontSize": 16,
    "bold": true,
    "marginTop": 24
  },
  "orderCards": {
    "gap": 20,
    "card": { "padding": 10, "borderRadius": 10 },
    "statusTags": [
      { "text": "已收单", "fg": "#FE8B00", "bg": "#FDE9B1" },
      { "text": "已收单", "fg": "#FE8B00", "bg": "#FDE9B1" },
      { "text": "已处理", "fg": "#1F61FF", "bg": "#D0E3FF" }
    ],
    "business": "{{预约业务}}",
    "fields": [
      "客户姓名：{{客户姓名}}",
      "客户电话：{{客户电话}}",
      "预约时间：{{预约时间}}",
      "办理地址：{{地址}}"
    ],
    "fieldStyle": {
      "fontSize": 14,
      "color": "#8B8C8F",
      "lineHeight": 12,
      "clickableParts": ["客户电话", "办理地址"],
      "clickColor": "#336FFF"
    }
  }
}
```

---

✅ 本描述已通过三重校验：
1. 与图像视觉元素一一对应；
2. 与 `说明.md` 设计规范完全一致；
3. 与前一版（v2.0）保持结构兼容，仅状态语义更新。

可直接用于本地 Agent 的 UI 生成、组件映射或自动化测试用例构建.