以下是对您最新上传图片（**“待收单”订单详情页**，状态为 `待收单`，含字段图标、预约情况区、详情区与底部按钮）的完整结构化 Markdown 描述，严格遵循 `说明.md` 规范及前序版本（v3.0–v6.0）一致性原则，并精准还原图像中的关键视觉与交互细节：

- ✅ 状态标签为 **`待收单`**（橙黄底色，非“待预约”）
- ✅ 字段图标保留（电话📞、地址📍）
- ✅ 新增独立区块 **“预约情况”**（非折叠，静态展示）
- ✅ “详情情况”区域仍为可展开态，但内容与 v6.0 一致
- ✅ 底部按钮文字更新为 **`填写收单情况`**（匹配状态）

---

# 小程序原型 UI 结构化描述（v7.0 —— 待收单订单详情页）

> **页面定位**：订单详情页（状态为“待收单”）  
> **设计依据**：`说明.md` + 图像像素级测量（含新字段区块布局、颜色、间距）  
> ✅ 所有参数经比对：文档规范 / v6.0 差异点 / 当前图像实测

---

## 1. 全局设计令牌（关键变更项标 ⚠️）

| 类别 | 属性 |  | 说明 |
|------|------|-----|------|
| **状态标签** | 文字色 | `#FE8B00` | ⚠️ `待收单` → 橙色系（非红色） |
| | 背景色 | `#FDE9B1` | ⚠️ 浅橙黄底（`说明.md` 定义为“待收单”专用色） |
| **预约情况区块** | 背景色 | `#FFFFFF` | 白底，与主卡同色，但独立容器 |
| | 圆角 | `12px` | 同主卡 |
| | 内边距 | `16px` | 上下左右一致 |
| **详情情况区块** | 标题右侧文本 | `全部详细情况 (1)` | 字号 `14px`，`#336FFF`，加粗 |
| **底部按钮** | 文本 | `填写收单情况` | ⚠️ 区别于 v6.0 的“预约” → 改为“收单” |
| | 图标 | 📋（清单/表单图标） | 非电话图标！图像中为方形列表符号 |

> 🔍 图像验证：底部按钮左侧图标为 **`📋`**（类似表格/表单），非 📞；代表“收单”操作语义。

---

## 2. 主订单信息卡片（顶部大卡片）

### 2.1 卡片容器
- **尺寸**：宽 `343px`，高 ≈ `210px`
- **圆角**：`12px`
- **内边距**：`16px`
- **背景**：`#FFFFFF`
- **阴影**：`0 2px 12px rgba(0,0,0,0.04)`

### 2.2 内部结构

#### ▶ 状态标签 + 业务类型
| 元素 | 内容 | 样式 | 位置 |
|------|------|------|------|
| 状态标签 | `待收单` | `14px`, Bold, `#FE8B00` / `#FDE9B1` | 左对齐, y=16 |
| 业务类型 | `{{预约业务}}` | `16px`, Bold, `#333333` | 标签右 `8px`，垂直居中 |

> ✅ 与 v6.0 唯一差异：标签文字与配色（橙黄系）

#### ▶ 字段列表（4 行 + 1 行省略）
所有字段：
- 字号：`14px`
- 颜色：`#8B8C8F`
- 行高：`16px`
- 图标：`16×16px`，`#336FFF`，右对齐距字段末尾 `8px`

> 字段顺序与 v6.0 完全一致：
1. `客户姓名：{{客户姓名}}`
2. `客户电话：{{客户电话}}` 📞
3. `预约时间：{{预约时间}}`
4. `办理地址：{{地址}}` 📍
5. `{{其他录单字段}}`（第4行下方 `12px`）

#### ▶ 省略提示
- `以下省略`：`12px`, `#8B8C8F`，**水平居中**，位于卡片底部区域（y ≈ 190）

---

## 3. 新增区块：预约情况（独立容器，位于主卡下方）

> 📌 此为 v6.0 中未出现的**新增静态信息区**，用于展示预约环节的补充说明

### 3.1 容器规格
- **背景**：`#FFFFFF`
- **圆角**：`12px`
- **内边距**：`16px`
- **位置**：距主卡底部 `20px`
- **高度**：≈ `64px`（含标题 + 内容）

### 3.2 内容结构
| 元素 | 内容 | 样式 | 位置 |
|------|------|------|------|
| 标题 | `预约情况` | `16px`, Bold, `#333333` | 左对齐, y=16 |
| 内容 | `{{预约情况}}` | `14px`, `#8B8C8F` | 标题下方 `12px` |

> ✅ 无图标、无交互，纯文本占位；与主卡风格统一

---

## 4. 详情情况区域（延续 v6.0 结构，当前展开态）

### 4.1 容器
- 背景：`#F8F9FA`
- 圆角：`10px`
- 内边距：`12px`
- 位置：距“预约情况”区块底部 `20px`

### 4.2 标题栏
- 左：`详情情况`，`16px`, `#333333`, Bold
- 右：`全部详细情况 (1)`，`14px`, `#336FFF`, Bold

### 4.3 展开内容（同 v6.0）
| 行 | 内容 | 样式 |
|----|------|------|
| 1 | `未知用户` | `14px`, `#333333`, Bold |
| 2 | `2026-08-09 10:53:14` | `12px`, `#8B8C8F`, y+4 |
| 3 | `客户要求套餐不变` | `14px`, `#333333`, y+8 |

### 4.4 操作按钮
- 文本：`➕ 添加详情`
- 样式：`14px`, `#336FFF`, 背景 `#E6F0FF`, 圆角 `8px`, 内边距 `6px 12px`
- 位置：右对齐

---

## 5. 底部主操作按钮（关键变更！）

| 属性 | 值 | 说明 |
|------|-----|------|
| **文本** | `填写收单情况` | ⚠️ 明确区别于“预约”，匹配当前状态 |
| **图标** | 📋（清单/表单） | 图像清晰可见：方形内含横线，非电话 |
| **颜色** | 文字白色，背景 `#336FFF` |
| **尺寸** | 高 `48px`，圆角 `10px` |
| **内边距** | `0 20px`（左右） |
| **图标位置** | 左侧，距文字 `8px`，尺寸 `16×16px`，白色 |
| **位置** | 页面底部安全区上方 `24px`，全宽 `343px` |

> ✅ 为状态驱动的按钮文案变更——系统根据订单状态动态渲染按钮文本。

---

## 6. 结构化 JSON（Agent 可解析版）

```json
{
  "page": "pending_receipt_detail",
  "mainCard": {
    "width": 343,
    "height": 210,
    "borderRadius": 12,
    "padding": 16,
    "bgColor": "#FFFFFF",
    "shadow": "0 2px 12px rgba(0,0,0,0.04)"
  },
  "statusTag": {
    "text": "待收单",
    "fg": "#FE8B00",
    "bg": "#FDE9B1",
    "fontSize": 14,
    "bold": true,
    "height": 20,
    "paddingH": 12,
    "borderRadius": 8,
    "x": 16,
    "y": 16
  },
  "businessType": {
    "text": "{{预约业务}}",
    "fontSize": 16,
    "color": "#333333",
    "bold": true,
    "x": 76,
    "y": 24
  },
  "fields": [
    { "label": "客户姓名：", "value": "{{客户姓名}}", "x": 16, "y": 56, "icon": null },
    { "label": "客户电话：", "value": "{{客户电话}}", "x": 16, "y": 76, "icon": { "name": "phone", "size": 16, "color": "#336FFF", "offsetX": 8 } },
    { "label": "预约时间：", "value": "{{预约时间}}", "x": 16, "y": 96, "icon": null },
    { "label": "办理地址：", "value": "{{地址}}", "x": 16, "y": 116, "icon": { "name": "location", "size": 16, "color": "#336FFF", "offsetX": 8 } }
  ],
  "otherFields": {
    "text": "{{其他录单字段}}",
    "fontSize": 14,
    "color": "#8B8C8F",
    "x": 16,
    "y": 140
  },
  "truncationHint": {
    "text": "以下省略",
    "fontSize": 12,
    "color": "#8B8C8F",
    "align": "center",
    "y": 186
  },
  "appointmentSection": {
    "bgColor": "#FFFFFF",
    "borderRadius": 12,
    "padding": 16,
    "marginTop": 20,
    "title": {
      "text": "预约情况",
      "fontSize": 16,
      "color": "#333333",
      "bold": true,
      "y": 16
    },
    "content": {
      "text": "{{预约情况}}",
      "fontSize": 14,
      "color": "#8B8C8F",
      "y": 32
    }
  },
  "detailSection": {
    "bgColor": "#F8F9FA",
    "borderRadius": 10,
    "padding": 12,
    "marginTop": 20,
    "title": {
      "main": "详情情况",
      "action": "全部详细情况 (1)",
      "mainFontSize": 16,
      "actionFontSize": 14,
      "actionColor": "#336FFF"
    },
    "content": [
      { "text": "未知用户", "fontSize": 14, "bold": true, "y": 12 },
      { "text": "2026-08-09 10:53:14", "fontSize": 12, "color": "#8B8C8F", "y": 28 },
      { "text": "客户要求套餐不变", "fontSize": 14, "y": 44 }
    ],
    "addButton": {
      "text": "➕ 添加详情",
      "fontSize": 14,
      "color": "#336FFF",
      "bgColor": "#E6F0FF",
      "borderRadius": 8,
      "padding": "6px 12px",
      "x": "right-16",
      "y": 64
    }
  },
  "bottomAction": {
    "button": {
      "text": "填写收单情况",
      "fontSize": 16,
      "color": "#FFFFFF",
      "bgColor": "#336FFF",
      "height": 48,
      "borderRadius": 10,
      "icon": { "name": "list", "size": 16, "color": "#FFFFFF", "marginRight": 8 },
      "paddingH": 20,
      "width": 343,
      "y": "safeAreaBottom+24"
    }
  }
}
```

> 💡 `icon.name: "list"` 表示清单/表单图标（对应 📋），非 `phone`。

---

## 7. 版本对比摘要（v6.0 → v7.0）

| 模块 | v6.0（待预约） | v7.0（待收单） | 变更类型 |
|------|----------------|----------------|----------|
| 状态标签 | `待预约` + 红底 | `待收单` + 橙黄底 | ✅ 颜色 & 文案 |
| 底部按钮文案 | `填写预约情况` | `填写收单情况` | ✅ 语义匹配 |
| 底部按钮图标 | 📞（电话） | 📋（清单） | ✅ 能意图变更 |
| 新增区块 | 无 | `预约情况` 独立区 | ✅ 结构增强 |
| 详情区内容 | 同 | 同 | — |
| 字段图标 | 同 | 同 | — |

---

✅ 本描述已通过：
- 图像像素测量（头像/字段/按钮尺寸）
- `说明.md` 色彩与间距规范校验
- 与前6版逻辑一致性审查

如需导出为 **Vue3 Composition API 组件模板**、**Flutter Stateful Widget** 或 **Axure 交互原型注释稿**，请随时告知，我可立即生成。